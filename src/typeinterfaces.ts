export type Project = {
  name: string;
  url: string;
  image: string;
  desc: string;
  projectKey: string;
  subtitle?: string; // Optional subtitle field
  role?: string; // Optional subtitle field
};
